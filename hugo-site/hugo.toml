baseURL = 'https://digitalartifacts.media/'
languageCode = 'en-us'
title = 'DIGITAL ARTIFACTS'

# Allow inline shortcodes (e.g., {{< siteparam "email" >}}) inside Markdown text
enableInlineShortcodes = true

[params]
  email = 'brett.gershon@icloud.com'
  description = 'sound + code + image + word — a collection of digital artifacts'
  twitter_username = 'btrailor'
  github_username = 'btrailor'
  # Add your music platform URLs here
  # bandcamp_url = 'https://yourusername.bandcamp.com'
  # spotify_url = 'https://open.spotify.com/artist/your-artist-id'
  # soundcloud_url = 'https://soundcloud.com/yourusername'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

# Enable SASS/SCSS processing
[build]
  writeStats = true
